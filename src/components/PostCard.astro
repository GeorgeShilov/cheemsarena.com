---
import type { BlogPost } from "@/data/blog";

const { post } = Astro.props as { post: BlogPost };
---

<article class="post-card">
  <img src={post.heroImage} alt="" loading="lazy" />
  <div>
    <p class="meta">{post.category} / {new Date(post.pubDate).toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" })}</p>
    <h3><a href={`/blog/${post.slug}/`}>{post.title}</a></h3>
    <p>{post.description}</p>
    <div class="tags">
      {post.tags.map((tag) => <span>{tag}</span>)}
    </div>
  </div>
</article>

<style>
  .post-card {
    display: grid;
    grid-template-rows: 180px 1fr;
    overflow: hidden;
    border: 1px solid rgba(245, 158, 11, 0.2);
    border-radius: 8px;
    background: rgba(20, 18, 42, 0.7);
    backdrop-filter: blur(12px);
  }

  img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    background: #14122a;
  }

  div {
    padding: 22px;
  }

  .meta {
    margin-bottom: 10px;
    color: #f59e0b;
    font-size: 13px;
    font-weight: 800;
  }

  h3 a:hover {
    color: #ec4899;
  }

  .tags {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-top: 18px;
    padding: 0;
  }

  span {
    border: 1px solid rgba(245, 158, 11, 0.2);
    border-radius: 999px;
    padding: 4px 9px;
    color: #a8a29e;
    font-size: 12px;
    font-weight: 700;
  }
</style>
